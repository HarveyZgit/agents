# Fixture Instructions

- Do not recreate deleted legacy files without user approval.
- Treat `src/retry-v2.ts` as a separate replacement design.
- Stop when a handoff points at the removed `src/retry.ts`.
